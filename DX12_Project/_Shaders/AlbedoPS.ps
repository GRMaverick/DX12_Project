Texture2D albedo : register(t0);

SamplerState samLinear : register(s0);

struct PSInput
{
	float4 Position		: SV_Position;
	float2 Texture		: TEXCOORD;
};

float4 main(PSInput _input) : SV_TARGET
{
	return albedo.Sample(samLinear, _input.Texture);
}